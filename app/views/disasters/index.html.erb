<ul>
     <% @disasters.each do |disaster|%>

     <li>
          <%= disaster.title %>
          <%= link_to "Show", disaster_path(disaster) %>
          <%= link_to "Edit", edit_disaster_path(disaster) %>
          <%= link_to "Delete", disaster_path(disaster), :method => :delete,:data => { :confirm => "Are you sure?" } %>
     </li>
     <% end %>
</ul>

<%= link_to "New Disaster Event", new_disaster_path %>