<%= form_for @comment, :url => disaster_comments_path(@disaster) do |f| %>
   <%= f.text_field :description %>
   <%= f.submit %>
   <%= link_to "Back to Last Page", disaster_path(@disaster) %>
<% end %>