<%= form_for @comment, :url => disaster_comment_path(@disaster, @comment), :html => { :method => :patch } do |f| %>
   <%= f.text_field :description %>
   <%= f.submit %>
   <%= link_to "Back to comment_index", disaster_comments_path(@disaster) %>
<% end %>